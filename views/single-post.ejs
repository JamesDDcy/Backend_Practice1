<!DOCTYPE html>
<html lang="en">

<%- include('includes/head') %>


    <body>
        <%- include('includes/header') %>

            <main>
                <h1>
                    <!-- equal sign makes sure that even if there is maliciouis code it is not rendered as actual html-->
                    <%= post.title %>
                        <article>
                            <small>posted by <%= post.username %> on <%= new Date(post.createdDate).getMonth() + 1 %>/
                                        <%= new Date(post.createdDate).getDate() %>/<%= new
                                                Date(post.createdDate).getFullYear() %></small>

                            <!-- %= is a safe escape | %- unescaped way of outputing -->
                            <div>
                                <%- filterUserHTML(post.body) %>
                            </div>
                        </article>

                        <% if (isAuthor) { %>
                            <a href="/edit-post/<%= post.id%>" class="button">Edit Post</a>
                            <form style="display: inline;" action="/delete-post<%= post.id %>" method="POST">
                                <button>Delete Post</button>
                            </form>
                            <% } %>

                </h1>

            </main>
            <%- include('includes/footer') %>

    </body>

</html>